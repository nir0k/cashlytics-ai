---
phase: 01-core-auth-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - auth.ts
  - src/lib/auth/password.ts
  - src/lib/validations/auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
autonomous: true
requirements:
  - AUTH-05
  - INFRA-01
  - INFRA-03
  - INFRA-05

must_haves:
  truths:
    - "next-auth@beta is installed with Drizzle adapter"
    - "auth.ts exports handlers, signIn, signOut, auth functions"
    - "Session type includes user.id for type-safe server actions"
    - "Password hashing uses bcrypt with 12 salt rounds"
    - "API route handler responds to /api/auth/* requests"
  artifacts:
    - path: "auth.ts"
      provides: "Central Auth.js v5 configuration"
      min_lines: 50
      contains: "export const { handlers, signIn, signOut, auth }"
    - path: "src/lib/auth/password.ts"
      provides: "bcrypt password utilities"
      exports: ["hashPassword", "verifyPassword"]
    - path: "src/lib/validations/auth.ts"
      provides: "Zod schema for credential validation"
      contains: "signInSchema"
    - path: "src/app/api/auth/[...nextauth]/route.ts"
      provides: "API route handler for Auth.js"
      exports: ["GET", "POST"]
  key_links:
    - from: "auth.ts"
      to: "src/lib/db"
      via: "DrizzleAdapter import"
      pattern: "DrizzleAdapter\\(db\\)"
    - from: "auth.ts"
      to: "session.user.id"
      via: "session callback"
      pattern: "session\\.user\\.id\\s*=\\s*token\\.sub"
---

<objective>
Install Auth.js v5 with Drizzle adapter and create core authentication configuration.

Purpose: Establish the authentication foundation that all other phases depend on.
Output: Working auth.ts configuration with password utilities and API route handler.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-core-auth-infrastructure/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Install Auth.js dependencies</name>
  <files>package.json</files>
  <action>
Install the required authentication packages:
- `next-auth@5.0.0-beta.30` — Auth.js v5 framework
- `@auth/drizzle-adapter@1.11.1` — Drizzle ORM adapter
- `bcrypt@6.0.0` — Password hashing (pure JS, Docker-friendly)

Run: `npm install next-auth@beta @auth/drizzle-adapter bcrypt`

Do NOT install @types/bcrypt (not needed for bcrypt@6.0.0 which includes types).
</action>
<verify>`npm ls next-auth @auth/drizzle-adapter bcrypt` shows all three installed</verify>
<done>package.json includes next-auth@beta, @auth/drizzle-adapter, and bcrypt</done>
</task>

<task type="auto">
  <name>Create password hashing utilities</name>
  <files>src/lib/auth/password.ts</files>
  <action>
Create `src/lib/auth/password.ts` with bcrypt utilities:

```typescript
import bcrypt from "bcrypt";

const SALT_ROUNDS = 12;

export async function hashPassword(password: string): Promise<string> {
  return bcrypt.hash(password, SALT_ROUNDS);
}

export async function verifyPassword(password: string, hashedPassword: string): Promise<boolean> {
  return bcrypt.compare(password, hashedPassword);
}
```

Create the directory `src/lib/auth/` if it doesn't exist.
</action>
<verify>`cat src/lib/auth/password.ts` contains hashPassword and verifyPassword exports</verify>
<done>Password utilities exist with bcrypt hashing at 12 salt rounds</done>
</task>

<task type="auto">
  <name>Create credential validation schemas</name>
  <files>src/lib/validations/auth.ts</files>
  <action>
Create `src/lib/validations/auth.ts` with Zod schemas for credential validation:

```typescript
import { z } from "zod";

export const signInSchema = z.object({
  email: z.string().min(1, "Email is required").email("Invalid email"),
  password: z
    .string()
    .min(1, "Password is required")
    .min(8, "Password must be at least 8 characters")
    .max(100, "Password is too long"),
});
```

Create the directory `src/lib/validations/` if it doesn't exist.
</action>
<verify>`cat src/lib/validations/auth.ts` contains signInSchema export</verify>
<done>Zod validation schemas exist for credential validation</done>
</task>

<task type="auto">
  <name>Create Auth.js v5 configuration</name>
  <files>auth.ts</files>
  <action>
Create `auth.ts` at project root with complete Auth.js v5 configuration:

Key requirements:

1. Import NextAuth, Credentials provider, DrizzleAdapter
2. Use `session: { strategy: "jwt" }` for Edge compatibility (CRITICAL - database strategy fails with proxy.ts)
3. Add `pages: { signIn: "/login" }` to establish redirect target
4. Extend Session interface with `user.id` via module augmentation
5. Implement Credentials provider with:
   - Zod validation via signInSchema
   - Password verification via verifyPassword
   - User lookup from database (users table will be created in Phase 2)
6. Add callbacks:
   - `jwt`: Store user.id in token
   - `session`: Copy user.id from token to session
   - `authorized`: Return !!auth for route protection

The users table does NOT exist yet (created in Phase 2). For now, the authorize function should be structured correctly but will return null until users exist. Add a TODO comment noting this.

Reference the complete auth.ts example in 01-RESEARCH.md Pattern 1 and Code Examples section.

CRITICAL: Use `AUTH_` prefix for env vars (not `NEXTAUTH_`) — Auth.js v5 convention.
</action>
<verify>`cat auth.ts` contains "export const { handlers, signIn, signOut, auth }" and "strategy: \"jwt\""</verify>
<done>Auth.js v5 configured with Drizzle adapter, JWT sessions, and extended session types</done>
</task>

<task type="auto">
  <name>Create API route handler</name>
  <files>src/app/api/auth/[...nextauth]/route.ts</files>
  <action>
Create `src/app/api/auth/[...nextauth]/route.ts`:

```typescript
import { handlers } from "@/auth";

export const { GET, POST } = handlers;
```

This is the standard Auth.js v5 pattern for App Router. The catch-all route handles all auth requests (signin, signout, session, csrf, etc.).

Create directories as needed: `src/app/api/auth/[...nextauth]/`
</action>
<verify>`cat src/app/api/auth/[...nextauth]/route.ts` contains GET and POST exports from handlers</verify>
<done>API route handler responds to /api/auth/\* requests</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm run build` to verify TypeScript compilation
2. Check that auth.ts exports all required functions
3. Verify JWT session strategy is used (not database)
</verification>

<success_criteria>

- next-auth@beta, @auth/drizzle-adapter, bcrypt installed
- auth.ts exists at project root with handlers, signIn, signOut, auth exports
- Session type extended with user.id via module augmentation
- Password utilities use bcrypt with 12 salt rounds
- API route handler created at src/app/api/auth/[...nextauth]/route.ts
- TypeScript build succeeds without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-auth-infrastructure/01-01-SUMMARY.md`
</output>
