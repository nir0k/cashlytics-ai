---
phase: 04-auth-ui-components
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/(auth)/register/page.tsx
  - src/components/organisms/register-form.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-07

must_haves:
  truths:
    - "Navigating to /register shows the registration form inside the split-panel auth layout"
    - "The form has email, password, and confirm password fields with inline errors"
    - "Submitting with mismatched passwords shows 'Passwords do not match' below confirmPassword"
    - "Submitting with a duplicate email shows 'An account with this email already exists' below the email field"
    - "Successful registration auto-logs in the user and redirects to /"
    - "The submit button is disabled with a spinner while the form is pending"
    - "A link to /login appears below the form"
  artifacts:
    - path: "src/app/(auth)/register/page.tsx"
      provides: "Register page server component at /register route"
      contains: "RegisterForm"
    - path: "src/components/organisms/register-form.tsx"
      provides: "Client form component with 3 fields and useActionState wiring"
      exports: ["RegisterForm"]
  key_links:
    - from: "src/components/organisms/register-form.tsx"
      to: "src/actions/auth-actions.ts"
      via: "import registerAction"
      pattern: "registerAction"
    - from: "src/app/(auth)/register/page.tsx"
      to: "src/components/organisms/register-form.tsx"
      via: "import RegisterForm"
      pattern: "RegisterForm"
---

<objective>
Build the register page and its client form component.

Purpose: Delivers /register (AUTH-07) and user self-registration (AUTH-01) with auto-login flow, matching the Vault aesthetic of the login page.
Output: (auth)/register/page.tsx, organisms/register-form.tsx
</objective>

<execution_context>
@/home/coder/.claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-auth-ui-components/04-01-SUMMARY.md
@src/components/organisms/login-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create register page and RegisterForm client component</name>
  <files>
    src/app/(auth)/register/page.tsx
    src/components/organisms/register-form.tsx
  </files>
  <action>
    Create two files. The register page mirrors the login page structure but uses `registerAction` and has 3 fields (email, password, confirmPassword).

    **`src/app/(auth)/register/page.tsx`** — thin server component:
    ```tsx
    import { RegisterForm } from "@/components/organisms/register-form";

    export default function RegisterPage() {
      return <RegisterForm />;
    }
    ```

    **`src/components/organisms/register-form.tsx`** — client form with three fields:

    Key requirements from CONTEXT.md (locked decisions):
    - Fields: email + password + confirmPassword (3 fields, in that order)
    - Password requirements shown as helper text below the password field: "Min. 8 characters, at least one number"
    - Errors appear inline below each field
    - Submit button disabled + spinner while pending (useFormStatus pattern — SubmitButton as separate component inside form)
    - "Already have an account? Sign in" link to /login below the card
    - Same Vault aesthetic as LoginForm: glass-elevated card, Syne heading, amber submit button

    ```tsx
    "use client";

    import { useActionState } from "react";
    import { useFormStatus } from "react-dom";
    import { registerAction, type AuthActionState } from "@/actions/auth-actions";
    import { Input } from "@/components/ui/input";
    import { Button } from "@/components/ui/button";
    import { Label } from "@/components/ui/label";
    import { Loader2 } from "lucide-react";
    import Link from "next/link";
    import Image from "next/image";

    const initialState: AuthActionState = {};

    function SubmitButton() {
      const { pending } = useFormStatus();
      return (
        <Button
          type="submit"
          disabled={pending}
          className="w-full bg-amber-500 hover:bg-amber-400 text-black font-semibold transition-all"
        >
          {pending ? (
            <>
              <Loader2 className="mr-2 h-4 w-4 animate-spin" />
              Creating account...
            </>
          ) : (
            "Create account"
          )}
        </Button>
      );
    }

    export function RegisterForm() {
      const [state, action] = useActionState(registerAction, initialState);

      return (
        <div className="w-full max-w-sm">
          {/* Logo above card */}
          <div className="mb-8 flex flex-col items-center gap-3">
            <Image
              src="/logo.svg"
              alt="Cashlytics"
              width={140}
              height={35}
              className="h-8 w-auto dark:brightness-0 dark:invert"
              priority
            />
          </div>

          {/* Glass card */}
          <div className="glass-elevated rounded-2xl p-8">
            <h1
              className="mb-1 text-2xl font-bold tracking-tight"
              style={{ fontFamily: "var(--font-syne)" }}
            >
              Create account
            </h1>
            <p className="text-sm text-muted-foreground/70 mb-6">
              Start tracking your finances
            </p>

            {/* Global error */}
            {state.error && (
              <div className="mb-4 rounded-lg bg-destructive/10 border border-destructive/20 px-4 py-3">
                <p className="text-sm text-destructive">{state.error}</p>
              </div>
            )}

            <form action={action} className="space-y-4">
              <div className="space-y-1.5">
                <Label htmlFor="email">Email</Label>
                <Input
                  id="email"
                  name="email"
                  type="email"
                  autoComplete="email"
                  placeholder="you@example.com"
                />
                {state.fieldErrors?.email && (
                  <p className="text-xs text-destructive">{state.fieldErrors.email}</p>
                )}
              </div>

              <div className="space-y-1.5">
                <Label htmlFor="password">Password</Label>
                <Input
                  id="password"
                  name="password"
                  type="password"
                  autoComplete="new-password"
                  placeholder="••••••••"
                />
                <p className="text-xs text-muted-foreground/60">
                  Min. 8 characters, at least one number
                </p>
                {state.fieldErrors?.password && (
                  <p className="text-xs text-destructive">{state.fieldErrors.password}</p>
                )}
              </div>

              <div className="space-y-1.5">
                <Label htmlFor="confirmPassword">Confirm password</Label>
                <Input
                  id="confirmPassword"
                  name="confirmPassword"
                  type="password"
                  autoComplete="new-password"
                  placeholder="••••••••"
                />
                {state.fieldErrors?.confirmPassword && (
                  <p className="text-xs text-destructive">{state.fieldErrors.confirmPassword}</p>
                )}
              </div>

              <div className="pt-1">
                <SubmitButton />
              </div>
            </form>
          </div>

          {/* Login link */}
          <p className="mt-4 text-center text-sm text-muted-foreground">
            Already have an account?{" "}
            <Link
              href="/login"
              className="text-amber-500 hover:text-amber-400 underline-offset-4 hover:underline transition-colors"
            >
              Sign in
            </Link>
          </p>
        </div>
      );
    }
    ```

    Note: The (auth)/layout.tsx from Plan 02 already provides the split-panel wrapper for this page. This plan only creates the page + form component.

    Note: proxy.ts already excludes `/register` from route protection (verified in Phase 1). The register page at `src/app/(auth)/register/page.tsx` resolves to `/register` — no proxy.ts changes needed.

  </action>
  <verify>
    Run `npx tsc --noEmit` — no errors in register-form.tsx and register/page.tsx.
    Confirm files exist: `src/app/(auth)/register/page.tsx` and `src/components/organisms/register-form.tsx`.
  </verify>
  <done>
    Register page at /register (AUTH-07) renders RegisterForm inside the auth split-panel layout.
    RegisterForm has email, password (with hint text), confirmPassword fields with inline errors.
    SubmitButton uses useFormStatus — spinner and disabled state during submission.
    On success, registerAction auto-logs in and redirects to /.
    TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero errors.
Confirm: `src/app/(auth)/register/page.tsx` imports and renders RegisterForm.
Confirm: `src/components/organisms/register-form.tsx` wires `registerAction` via `useActionState`.
Confirm: All 3 form fields present (email, password, confirmPassword) with individual error displays.
</verification>

<success_criteria>

- /register resolves to register page using the (auth) layout (no sidebar, no header)
- RegisterForm has email + password + confirmPassword with inline errors per field
- "An account with this email already exists" shown as email field error for duplicates
- "Passwords do not match" shown as confirmPassword error when passwords differ
- Successful registration auto-logs in and redirects to /
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/04-auth-ui-components/04-03-SUMMARY.md`
</output>
