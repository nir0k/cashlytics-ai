---
phase: 03-server-actions-refactor
plan: "05"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - src/actions/analytics-actions.ts
  - src/actions/dashboard-actions.ts
  - src/actions/account-detail-actions.ts
  - src/actions/search-actions.ts
  - src/actions/forecast-actions.ts
autonomous: true
requirements:
  - AUTHZ-03
  - DATA-01

must_haves:
  truths:
    - "Every analytics function (getMonthlyOverview, getMonthlyTrend, getCategoryBreakdown, etc.) requires a valid session"
    - "Every query in analytics-actions.ts filters by userId — including all three parallel sub-queries in Promise.all"
    - "dashboard-actions.ts getDashboardStats shows only the authenticated user's financial data"
    - "getRecentTransactions in dashboard shows only user's transactions"
    - "No analytics or dashboard function leaks data from other users"
    - "Unauthenticated calls return { success: false, error: 'Unauthorized' }"
  artifacts:
    - path: "src/actions/analytics-actions.ts"
      provides: "Analytics queries with full userId filtering on every sub-query"
      exports:
        [
          "getMonthlyOverview",
          "getForecast",
          "getCategoryBreakdown",
          "getMonthlyTrend",
          "getExpensesByCategory",
          "getIncomeVsExpensesByMonth",
          "getSavingsProgress",
          "getSubscriptions",
          "getMonthlyPaymentsCalendar",
          "getNormalizedMonthlyExpenses",
        ]
    - path: "src/actions/dashboard-actions.ts"
      provides: "Dashboard stats with userId filtering"
      exports:
        [
          "getDashboardStats",
          "getCategoryBreakdown",
          "getRecentTransactions",
          "getAccounts",
          "getUpcomingPayments",
        ]
    - path: "src/actions/account-detail-actions.ts"
      provides: "Account detail queries filtered by userId"
    - path: "src/actions/search-actions.ts"
      provides: "Search results filtered by userId"
    - path: "src/actions/forecast-actions.ts"
      provides: "Forecast data filtered by userId (if standalone file)"
  key_links:
    - from: "src/actions/analytics-actions.ts"
      to: "expenses/incomes/dailyExpenses tables"
      via: "eq(expenses.userId, userId) in every .from(expenses) query — including Promise.all sub-queries"
      pattern: "eq\\(expenses\\.userId|eq\\(incomes\\.userId|eq\\(dailyExpenses\\.userId"
    - from: "src/actions/dashboard-actions.ts"
      to: "accounts/expenses/incomes/dailyExpenses tables"
      via: "eq(table.userId, userId) on every query"
      pattern: "eq\\(accounts\\.userId|eq\\(incomes\\.userId|eq\\(expenses\\.userId|eq\\(dailyExpenses\\.userId"
---

<objective>
Refactor analytics, dashboard, account-detail, search, and forecast server actions — the files that currently have NO auth at all.

Purpose: These read-only aggregate functions expose financial summaries. Without auth they leak data across all users. analytics-actions.ts is the most complex: it has multiple functions with Promise.all parallel queries where EVERY sub-query must include userId.
Output: All analytics/dashboard/supporting action files secured with requireAuth and userId-scoped queries.
</objective>

<execution_context>
@/home/coder/.claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-actions-refactor/03-RESEARCH.md
@src/actions/analytics-actions.ts
@src/actions/dashboard-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor analytics-actions.ts — add auth and userId to every query</name>
  <files>src/actions/analytics-actions.ts</files>
  <action>
    analytics-actions.ts has NO auth currently. Add requireAuth to every exported function. The critical pitfall (from research): this file uses Promise.all with multiple parallel queries — EVERY sub-query must include userId.

    1. Add `import { requireAuth } from "@/lib/auth/require-auth"` at the top. Add `and` to the drizzle-orm imports (it's already imported in some functions but may not cover all).

    2. Apply requireAuth guard pattern to each function. The userId must be threaded into every sub-query.

    **getMonthlyOverview(month, year, accountId?):**
    - Add requireAuth at top.
    - expensesResult query: add `eq(expenses.userId, userId)` to the WHERE `and(...)`.
    - incomesResult query: add `eq(incomes.userId, userId)` to the WHERE `and(...)`.
    - dailyExpensesResult query: add `eq(dailyExpenses.userId, userId)` to the WHERE `and(...)`.
    - Note: getForecast calls getMonthlyOverview internally — no direct auth needed in getForecast since getMonthlyOverview handles it. However, add requireAuth to getForecast independently too (defense in depth).

    **getForecast(months):**
    - Add requireAuth at top and return Unauthorized on failure.
    - getForecast calls getMonthlyOverview — but getMonthlyOverview requires its own session. Since getForecast is a server action too, its requireAuth is independent.

    **getCategoryBreakdown(startDate, endDate, accountId?):**
    - Add requireAuth at top.
    - dailyExpensesResult query: add `eq(dailyExpenses.userId, userId)` to WHERE.
    - periodicExpensesResult query: add `eq(expenses.userId, userId)` to WHERE.

    **getNormalizedMonthlyExpenses():**
    - Add requireAuth at top.
    - expensesResult query: add `eq(expenses.userId, userId)` to the bare `.from(expenses).leftJoin(...).leftJoin(...)` — add `.where(eq(expenses.userId, userId))`.

    **getMonthlyTrend(months?):**
    - Add requireAuth at top.
    - This uses `Promise.all([db...incomes..., db...expenses..., db...dailyExpenses...])`.
    - EVERY sub-query in the array must include userId:
      - incomes query: add `and(lte(incomes.startDate, rangeEnd), eq(incomes.userId, userId))` to WHERE.
      - expenses query: add `eq(expenses.userId, userId)` to the existing `and(lte(...), sql\`...\`)` condition.
      - dailyExpenses query: add `eq(dailyExpenses.userId, userId)` to the existing `and(gte(...), lte(...))`.

    **getExpensesByCategory(year?, month?):**
    - Add requireAuth at top.
    - The single query on dailyExpenses: add `eq(dailyExpenses.userId, userId)` to the WHERE `and(gte(...), lte(...))`.

    **getIncomeVsExpensesByMonth(year?):**
    - Add requireAuth at top.
    - This uses `Promise.all` with 3 queries — add userId to each:
      - incomes: `and(lte(incomes.startDate, yearEnd), eq(incomes.userId, userId))`.
      - expenses: add `eq(expenses.userId, userId)` to the `and(lte(...), sql\`...\`)`.
      - dailyExpenses: `and(gte(...), lte(...), eq(dailyExpenses.userId, userId))`.

    **getSavingsProgress():**
    - Add requireAuth at top.
    - This uses `Promise.all` with 3 queries:
      - activeIncomes: add `and(lte(incomes.startDate, end), eq(incomes.userId, userId))`.
      - activeExpenses: add `eq(expenses.userId, userId)` to existing `and(lte(...), sql\`...\`)`.
      - dailyResult (SUM query): add `and(gte(...), lte(...), eq(dailyExpenses.userId, userId))` to WHERE.

    **getSubscriptions():**
    - Add requireAuth at top.
    - Add `and(eq(expenses.isSubscription, true), eq(expenses.userId, userId))` to WHERE.

    **getMonthlyPaymentsCalendar(year, month):**
    - Add requireAuth at top.
    - This uses `Promise.all` with 3 queries:
      - expensesResult: add `eq(expenses.userId, userId)` to WHERE (currently `sql\`${expenses.endDate} IS NULL OR...\``). Change to `and(sql\`${expenses.endDate} IS NULL OR ${expenses.endDate} >= ${monthStart.toISOString()}\`, eq(expenses.userId, userId))`.
      - dailyExpensesResult: add `eq(dailyExpenses.userId, userId)` to the `and(gte(...), lte(...))`.
      - incomesResult: add `and(lte(incomes.startDate, monthEnd), eq(incomes.userId, userId))`.

    After each function, verify mentally: does every `.from(table)` that returns user financial data have `eq(table.userId, userId)` in its WHERE?

  </action>
  <verify>
    Run: npx tsc --noEmit from /home/coder/cashlytics — no errors.
    Run: grep -c "requireAuth" src/actions/analytics-actions.ts — should show count >= 10 (one per exported function, some functions call it once).
    Run: grep -c "eq.*\.userId" src/actions/analytics-actions.ts — should show a high count (every sub-query has userId filter).
    Run: grep -n "getCurrentUserId\|user-id" src/actions/analytics-actions.ts — empty output (this file never used it, just verify it's not there).
  </verify>
  <done>
    analytics-actions.ts has requireAuth at the top of every exported function, every query that reads from expenses/incomes/dailyExpenses/categories/accounts includes eq(table.userId, userId) in WHERE — including all Promise.all parallel sub-queries, TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor dashboard-actions.ts, account-detail-actions.ts, search-actions.ts, forecast-actions.ts</name>
  <files>src/actions/dashboard-actions.ts, src/actions/account-detail-actions.ts, src/actions/search-actions.ts, src/actions/forecast-actions.ts</files>
  <action>
    These files currently have NO auth. Apply requireAuth and userId filtering to all functions.

    **dashboard-actions.ts:**

    1. Add `import { requireAuth } from "@/lib/auth/require-auth"` at top. Add `and` to drizzle-orm imports.

    2. **getDashboardStats()**: Add requireAuth at top. Thread userId into every sub-query:
       - accountsResult: add `eq(accounts.userId, userId)` to WHERE (currently no WHERE — gets all accounts total). Change to: `.from(accounts).where(eq(accounts.userId, userId))`.
       - activeIncomes: add `and(lte(incomes.startDate, now), eq(incomes.userId, userId))` to WHERE.
       - oneTimeIncomes: add `eq(incomes.userId, userId)` to the `and(...)`.
       - currentMonthDailyExpenses: add `and(gte(dailyExpenses.date, currentMonthStart), eq(dailyExpenses.userId, userId))`.
       - activeExpenses: add `eq(expenses.userId, userId)` to the `and(lte(...), sql\`...\`)`.
       - lastMonthDailyExp: add `eq(dailyExpenses.userId, userId)` to the `and(gte(...), lte(...))`.

    3. **getCategoryBreakdown(startDate?)**: Add requireAuth at top.
       - dailyExpensesResult: add `eq(dailyExpenses.userId, userId)` to WHERE.
       - periodicExpensesResult: add `eq(expenses.userId, userId)` to WHERE.

    4. **getRecentTransactions(limit?)**: Add requireAuth at top. Add userId filter to `db.query.dailyExpenses.findMany` — add `where: eq(dailyExpenses.userId, userId)` in the findMany options.

    5. **getAccounts()**: Add requireAuth at top. Add `eq(accounts.userId, userId)` to WHERE: `.from(accounts).where(eq(accounts.userId, userId)).orderBy(accounts.name)`.

    6. **getUpcomingPayments(days?)**: Add requireAuth at top.
       - activeExpenses query: add `eq(expenses.userId, userId)` to WHERE `and(sql\`...\`, eq(expenses.userId, userId))`.
       - upcomingDailyExpenses query: add `eq(dailyExpenses.userId, userId)` to the `and(gte(...), lte(...))`.

    ---

    **account-detail-actions.ts** (check file contents and apply):
    Read the file first. Apply requireAuth to all functions and add userId filtering. Key patterns:
    - Any query reading from accounts: add `and(eq(accounts.id, id), eq(accounts.userId, userId))`.
    - Any query reading transactions for an account: add userId filter to the expense/income/transfer queries.

    **search-actions.ts** (check file contents and apply):
    Read the file first. Apply requireAuth at top of all search functions. Add `eq(table.userId, userId)` to every search query's WHERE clause so search results are scoped to the authenticated user.

    **forecast-actions.ts** (check file contents):
    Check if this is a standalone file or if its functions were merged into analytics-actions.ts. If it exists and has functions, apply the same requireAuth + userId filtering pattern.

    For all files:
    - Keep all existing logic, error messages, and revalidatePath calls unchanged
    - Only add: requireAuth guard at top + userId filter in WHERE clauses
    - Return `{ success: false, error: "Unauthorized" }` on auth failure

  </action>
  <verify>
    Run: npx tsc --noEmit from /home/coder/cashlytics — no errors.
    Run: grep -rn "getCurrentUserId" src/actions/dashboard-actions.ts src/actions/account-detail-actions.ts src/actions/search-actions.ts src/actions/forecast-actions.ts — empty output.
    Run: grep -c "requireAuth" src/actions/dashboard-actions.ts — count >= 5 (one per function).
    Run: grep -c "eq.*\.userId" src/actions/dashboard-actions.ts — positive count showing userId filters.
  </verify>
  <done>
    All four files have requireAuth guards on every exported function, every query is scoped to the authenticated user's userId, no getCurrentUserId references remain, TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
Run from /home/coder/cashlytics:
1. `npx tsc --noEmit` — no TypeScript errors
2. `grep -rn "getCurrentUserId" src/actions/` — empty output across ALL action files
3. `grep -rn "requireAuth" src/actions/ | wc -l` — high count (every exported function in every action file)
4. Spot check: `grep -n "eq.*userId" src/actions/analytics-actions.ts | wc -l` — count >= 10 (many queries filtered)
5. `grep -rn "user-id" src/actions/` — empty output (no imports of user-id helper remain)
</verification>

<success_criteria>

- analytics-actions.ts: requireAuth in every function, userId in every sub-query including Promise.all queries
- dashboard-actions.ts: requireAuth in every function, userId in every query
- account-detail-actions.ts, search-actions.ts, forecast-actions.ts: requireAuth in every function, userId scoping
- Zero references to getCurrentUserId across all src/actions/ files
- Zero references to user-id import in src/actions/
- TypeScript compiles cleanly (npx tsc --noEmit)
- Phase 3 complete: AUTHZ-02, AUTHZ-03, DATA-01, DATA-10 all implemented
  </success_criteria>

<output>
After completion, create `.planning/phases/03-server-actions-refactor/03-05-SUMMARY.md`
</output>
