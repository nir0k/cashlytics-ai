---
phase: 04-auth-ui-components
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/components/organisms/login-form.tsx
autonomous: true
requirements:
  - AUTH-02
  - AUTH-06

must_haves:
  truths:
    - "Navigating to /login shows a full-screen split-panel page with no sidebar or header"
    - "The login form has email + password fields with inline error display"
    - "Submitting valid credentials redirects to the dashboard at /"
    - "Submitting invalid credentials shows the generic message 'Invalid email or password'"
    - "The submit button is disabled with a spinner while the form is pending"
    - "A link to /register appears below the form"
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "Split-panel auth layout with Vault branding left panel + form right panel"
      min_lines: 40
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page server component at /login route"
      contains: "LoginForm"
    - path: "src/components/organisms/login-form.tsx"
      provides: "Client form component with useActionState + useFormStatus spinner"
      exports: ["LoginForm"]
  key_links:
    - from: "src/components/organisms/login-form.tsx"
      to: "src/actions/auth-actions.ts"
      via: "import loginAction"
      pattern: "loginAction"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/organisms/login-form.tsx"
      via: "import LoginForm"
      pattern: "LoginForm"
---

<objective>
Build the (auth) route group layout and the login page with its client form component.

Purpose: Delivers /login (AUTH-06) as a fully functional page with the Vault split-panel design — completely separate from the dashboard layout.
Output: (auth)/layout.tsx, (auth)/login/page.tsx, organisms/login-form.tsx
</objective>

<execution_context>
@/home/coder/.claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-auth-ui-components/04-01-SUMMARY.md
@src/app/globals.css
@src/components/layout/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create (auth) route group layout with split-panel design</name>
  <files>src/app/(auth)/layout.tsx</files>
  <action>
    Create `src/app/(auth)/layout.tsx` as a server component (no 'use client'). This layout applies to ALL pages in the (auth) route group (/login, /register) and must NOT inherit the dashboard's sidebar/header.

    The layout is a full-screen horizontal split:
    - **Left panel** (hidden on mobile, shown lg+): branding panel with Vault aesthetic. Approximately 45% width on lg, 40% on xl. Background `#08080a`. Include `.vault-grid` overlay, amber blob ambients, and a centered branding block with the logo + tagline.
    - **Right panel** (flex-1): white/background panel that centers the `{children}` form content.

    ```tsx
    import type { Metadata } from "next";
    import Image from "next/image";

    export const metadata: Metadata = {
      title: "Cashlytics — Auth",
    };

    export default function AuthLayout({ children }: { children: React.ReactNode }) {
      return (
        <div className="flex min-h-screen">
          {/* Left panel — branding (hidden on mobile) */}
          <div className="relative hidden lg:flex lg:w-[45%] xl:w-[40%] flex-col items-center justify-center overflow-hidden bg-[#08080a] p-12">
            {/* Vault grid overlay */}
            <div className="vault-grid absolute inset-0 pointer-events-none" />

            {/* Amber ambient blobs */}
            <div
              className="absolute top-[-20%] left-[-20%] h-[70%] w-[70%] rounded-full pointer-events-none"
              style={{
                background:
                  "radial-gradient(ellipse at center, rgba(245,158,11,0.12) 0%, transparent 70%)",
              }}
            />
            <div
              className="absolute bottom-[-10%] right-[-10%] h-[50%] w-[50%] rounded-full pointer-events-none"
              style={{
                background:
                  "radial-gradient(ellipse at center, rgba(245,158,11,0.07) 0%, transparent 70%)",
              }}
            />

            {/* Branding content */}
            <div className="relative z-10 flex flex-col items-center gap-8 text-center">
              <Image
                src="/logo.svg"
                alt="Cashlytics"
                width={160}
                height={40}
                className="h-9 w-auto brightness-0 invert"
                priority
              />
              <div className="space-y-3 max-w-xs">
                <p
                  className="text-2xl font-bold text-white tracking-tight leading-snug"
                  style={{ fontFamily: "var(--font-syne)" }}
                >
                  Your finances,
                  <br />
                  under control.
                </p>
                <p
                  className="text-sm text-white/40 leading-relaxed"
                  style={{ fontFamily: "var(--font-jakarta)" }}
                >
                  AI-powered budget tracking for people who take their money seriously.
                </p>
              </div>

              {/* Decorative amber accent line */}
              <div className="h-px w-16 bg-gradient-to-r from-transparent via-amber-500/60 to-transparent" />
            </div>
          </div>

          {/* Right panel — form area */}
          <div className="flex flex-1 items-center justify-center bg-background p-8">
            {children}
          </div>
        </div>
      );
    }
    ```

    Use `Image` from `next/image` for the logo. The logo at `/logo.svg` is white-on-dark — `brightness-0 invert` is a Tailwind filter utility that inverts the dark logo for use on the dark left panel.

  </action>
  <verify>Run `npx tsc --noEmit` — no errors. Confirm file exists at `src/app/(auth)/layout.tsx`.</verify>
  <done>AuthLayout renders full-screen split with branding left panel and form right panel. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create login page and LoginForm client component</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/components/organisms/login-form.tsx
  </files>
  <action>
    Create two files:

    **`src/app/(auth)/login/page.tsx`** — thin server component that renders LoginForm:
    ```tsx
    import { LoginForm } from "@/components/organisms/login-form";

    export default function LoginPage() {
      return <LoginForm />;
    }
    ```

    **`src/components/organisms/login-form.tsx`** — client form component using `useActionState` + `useFormStatus`:

    Critical implementation notes:
    - `useActionState` is from `"react"` (NOT `"react-dom"`) — React 19 / Next.js 15
    - `useFormStatus` is from `"react-dom"` — must be used inside a component that is rendered inside `<form>`
    - Extract `SubmitButton` as its own component (child of the form) so `useFormStatus` works correctly
    - `loginAction` is imported from `"@/actions/auth-actions"` (Plan 01)
    - Use `glass-elevated` CSS utility class (from globals.css) for the card — consistent with Vault aesthetic
    - Logo mark above the card: small diamond SVG or just the logo image — use the existing `/logo.svg` via `next/image`
    - Link to `/register` below the card

    ```tsx
    "use client";

    import { useActionState } from "react";
    import { useFormStatus } from "react-dom";
    import { loginAction, type AuthActionState } from "@/actions/auth-actions";
    import { Input } from "@/components/ui/input";
    import { Button } from "@/components/ui/button";
    import { Label } from "@/components/ui/label";
    import { Loader2 } from "lucide-react";
    import Link from "next/link";
    import Image from "next/image";

    const initialState: AuthActionState = {};

    function SubmitButton() {
      const { pending } = useFormStatus();
      return (
        <Button
          type="submit"
          disabled={pending}
          className="w-full bg-amber-500 hover:bg-amber-400 text-black font-semibold transition-all"
        >
          {pending ? (
            <>
              <Loader2 className="mr-2 h-4 w-4 animate-spin" />
              Signing in...
            </>
          ) : (
            "Sign in"
          )}
        </Button>
      );
    }

    export function LoginForm() {
      const [state, action] = useActionState(loginAction, initialState);

      return (
        <div className="w-full max-w-sm">
          {/* Logo above card */}
          <div className="mb-8 flex flex-col items-center gap-3">
            <Image
              src="/logo.svg"
              alt="Cashlytics"
              width={140}
              height={35}
              className="h-8 w-auto dark:brightness-0 dark:invert"
              priority
            />
          </div>

          {/* Glass card */}
          <div className="glass-elevated rounded-2xl p-8">
            <h1
              className="mb-1 text-2xl font-bold tracking-tight"
              style={{ fontFamily: "var(--font-syne)" }}
            >
              Welcome back
            </h1>
            <p className="text-sm text-muted-foreground/70 mb-6">
              Sign in to your account
            </p>

            {/* Global error */}
            {state.error && (
              <div className="mb-4 rounded-lg bg-destructive/10 border border-destructive/20 px-4 py-3">
                <p className="text-sm text-destructive">{state.error}</p>
              </div>
            )}

            <form action={action} className="space-y-4">
              <div className="space-y-1.5">
                <Label htmlFor="email">Email</Label>
                <Input
                  id="email"
                  name="email"
                  type="email"
                  autoComplete="email"
                  placeholder="you@example.com"
                />
                {state.fieldErrors?.email && (
                  <p className="text-xs text-destructive">{state.fieldErrors.email}</p>
                )}
              </div>

              <div className="space-y-1.5">
                <Label htmlFor="password">Password</Label>
                <Input
                  id="password"
                  name="password"
                  type="password"
                  autoComplete="current-password"
                  placeholder="••••••••"
                />
                {state.fieldErrors?.password && (
                  <p className="text-xs text-destructive">{state.fieldErrors.password}</p>
                )}
              </div>

              <div className="pt-1">
                <SubmitButton />
              </div>
            </form>
          </div>

          {/* Register link */}
          <p className="mt-4 text-center text-sm text-muted-foreground">
            Don&apos;t have an account?{" "}
            <Link
              href="/register"
              className="text-amber-500 hover:text-amber-400 underline-offset-4 hover:underline transition-colors"
            >
              Register
            </Link>
          </p>
        </div>
      );
    }
    ```

  </action>
  <verify>
    Run `npx tsc --noEmit` — no errors.
    Run `npm run build` or `npx next build` (if available) — no build errors.
    Confirm files exist: `src/app/(auth)/login/page.tsx` and `src/components/organisms/login-form.tsx`.
  </verify>
  <done>
    Login page at /login (AUTH-06) renders LoginForm inside the auth layout.
    LoginForm has email/password fields, inline errors, amber submit button with spinner, and link to /register.
    TypeScript compiles. Build succeeds.
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero errors.
Confirm: `src/app/(auth)/layout.tsx` exists with split-panel design.
Confirm: `src/app/(auth)/login/page.tsx` renders LoginForm.
Confirm: `src/components/organisms/login-form.tsx` exports LoginForm with useActionState wiring.
</verification>

<success_criteria>

- /login resolves to login page inside the (auth) route group (no sidebar, no header)
- LoginForm renders with email, password fields + SubmitButton component (useFormStatus inside form)
- useActionState wired to loginAction from Plan 01
- Vault aesthetic applied: glass-elevated card, amber submit button, Syne heading
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/04-auth-ui-components/04-02-SUMMARY.md`
</output>
