---
phase: 04-auth-ui-components
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/layout/app-sidebar.tsx
autonomous: false
requirements:
  - AUTH-04

must_haves:
  truths:
    - "A 'Sign out' button is visible in the sidebar footer section"
    - "Clicking 'Sign out' calls logoutAction and redirects the user to /login"
    - "The logout button visually matches the Vault sidebar style (same icon box + text pattern as nav items)"
  artifacts:
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Logout button in SidebarFooter above the version badge"
      contains: "logoutAction"
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/actions/auth-actions.ts"
      via: "import logoutAction"
      pattern: "logoutAction"
---

<objective>
Add the logout button to the sidebar footer, wired to the logoutAction server action. Then verify the complete Phase 4 auth flow end-to-end.

Purpose: Completes AUTH-04 — users can sign out from the main application nav and are redirected to /login. The checkpoint validates the entire phase 4 auth round-trip.
Output: Updated app-sidebar.tsx with logout form + button
</objective>

<execution_context>
@/home/coder/.claude/get-shit-done/workflows/execute-plan.md
@/home/coder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-auth-ui-components/04-01-SUMMARY.md
@src/components/layout/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logout button to SidebarFooter</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <action>
    Modify `src/components/layout/app-sidebar.tsx` to add a logout button in the `SidebarFooter` section.

    The sidebar is already a `'use client'` component. Since we need to call a server action (`logoutAction`), use a `<form>` with `action={logoutAction}` — this is the correct pattern for calling server actions from client components without manual onClick handlers.

    Changes:
    1. Add `import { logoutAction } from "@/actions/auth-actions";` to the imports
    2. Add `LogOut` to the lucide-react import (already has other Lucide icons)
    3. In the `SidebarFooter`, add a logout form BETWEEN the Tools nav menu and the version badge

    The logout button should visually match the existing nav item style: icon box on the left, label text on the right, subtle hover state. Use a slightly different treatment (muted tone) to differentiate it from nav items:

    ```tsx
    {/* Logout button */}
    <form action={logoutAction} className="mt-2">
      <button
        type="submit"
        className="group flex h-10 w-full items-center gap-3 rounded-xl px-3 text-sm font-medium text-muted-foreground/70 hover:text-foreground hover:bg-white/[0.05] transition-all duration-200"
      >
        <div className="flex h-7 w-7 shrink-0 items-center justify-center rounded-lg bg-transparent group-hover:bg-white/[0.06] transition-all duration-200">
          <LogOut className="h-4 w-4" />
        </div>
        <span style={{ fontFamily: "var(--font-jakarta)" }}>Sign out</span>
      </button>
    </form>
    ```

    Place this block directly after the closing `</SidebarMenu>` of the bottomNavItems, before the `{/* Version badge */}` div. The footer structure becomes:
    1. "Tools" separator label
    2. SidebarMenu with settings + assistant nav items
    3. Logout form (new)
    4. Version badge div

    Do NOT remove or modify any existing nav items, the version badge, or the separator-label. Only add the new form block.

  </action>
  <verify>
    Run `npx tsc --noEmit` — no errors in app-sidebar.tsx.
    Confirm `logoutAction` is imported and used in a `<form action={logoutAction}>`.
    Confirm `LogOut` icon is imported from lucide-react.
  </verify>
  <done>
    app-sidebar.tsx has logout button in SidebarFooter.
    Button uses form action pattern wired to logoutAction.
    Visual style matches Vault sidebar aesthetic (icon box + label, hover state).
    TypeScript compiles without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 4 auth flow end-to-end</name>
  <files>none</files>
  <action>
    Human verifies the complete auth round-trip by testing all auth flows in the browser.
    Run `npm run dev` to start the development server before verifying.
  </action>
  <verify>
    Start the dev server: `npm run dev`

    1. **Unauthenticated access** — Visit http://localhost:3000/dashboard — should redirect to /login
    2. **Login page layout** — Visit http://localhost:3000/login — should show split-panel (branding left, form right), NO sidebar/header
    3. **Register flow** — Visit http://localhost:3000/register — fill in email + password + confirm, submit — should redirect to dashboard
    4. **Login flow** — Log out, visit /login, enter credentials — should redirect to dashboard
    5. **Error handling** — On /login, enter wrong password — should show "Invalid email or password" inline
    6. **Duplicate email** — On /register, use already-registered email — should show "An account with this email already exists" below email field
    7. **Password mismatch** — On /register, enter mismatched passwords — should show "Passwords do not match" below confirmPassword
    8. **Logout** — From dashboard, click Sign out in sidebar footer — should redirect to /login
    9. **Session persistence** — Log in, refresh page — should stay logged in

  </verify>
  <done>All 9 auth flow checks pass. User types "approved" to proceed.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero TypeScript errors across all modified files.
Manual verification: Complete auth round-trip (register → redirect to dashboard → logout → redirect to login → login → redirect to dashboard).
</verification>

<success_criteria>

- Logout button visible in sidebar footer with LogOut icon
- `<form action={logoutAction}>` pattern used (correct server action wiring from client component)
- Clicking Sign out redirects to /login
- Full auth round-trip works: register → dashboard → logout → login → dashboard
- TypeScript compiles without errors across all Phase 4 files
  </success_criteria>

<output>
After completion, create `.planning/phases/04-auth-ui-components/04-04-SUMMARY.md`
</output>
