---
phase: 08-email-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/emails/base-template.tsx
  - src/emails/reset-password.tsx
autonomous: true
requirements:
  - TEMPLATES-01
  - TEMPLATES-03
  - TEMPLATES-04
  - TEMPLATES-05
user_setup: []

must_haves:
  truths:
    - Reset password email renders with Vault dark theme (dark background #08080a, amber accents #f59e0b)
    - All styles are inline using style={{ }} props (no CSS classes)
    - Reset email includes 1-hour expiry notice
    - Plaintext version can be generated from the same component
  artifacts:
    - path: src/emails/base-template.tsx
      provides: Vault-branded email wrapper with dark theme colors
      exports: ["BaseEmail", "VAULT_COLORS"]
    - path: src/emails/reset-password.tsx
      provides: Reset password email with token link and expiry
      exports: ["ResetPasswordEmail"]
  key_links:
    - from: src/emails/reset-password.tsx
      to: src/emails/base-template.tsx
      via: import BaseEmail
      pattern: import.*BaseEmail.*from.*base-template
---

<objective>
Create Vault-branded email infrastructure with reset password template.

Purpose: Establish reusable email component system with consistent Vault dark theme branding for all transactional emails.
Output: Base template component, reset password email, @react-email packages installed
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-email-templates/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Install @react-email packages</name>
  <files>package.json</files>
  <action>
Install the React Email packages for server-side email rendering:
```bash
npm install @react-email/components@1.0.8 @react-email/render@2.0.4
```

These packages provide:

- @react-email/components: All email primitives (Html, Body, Container, Button, etc.) with inline style support
- @react-email/render: Renders React components to HTML and plaintext

Do NOT install @react-email/tailwind - use inline styles directly as per research findings.
</action>
<verify>grep -E '"@react-email/(components|render)"' package.json</verify>
<done>Both @react-email/components@1.0.8 and @react-email/render@2.0.4 are in package.json dependencies</done>
</task>

<task type="auto">
  <name>Create Vault-branded base email template</name>
  <files>src/emails/base-template.tsx</files>
  <action>
Create src/emails/base-template.tsx with Vault dark theme styling.

Use these exact VAULT_COLORS (extracted from globals.css):

```typescript
const VAULT_COLORS = {
  background: "#08080a",
  foreground: "#f0ede8",
  primary: "#f59e0b",
  primaryForeground: "#0a0a0b",
  muted: "#8a8070",
  border: "rgba(255, 255, 255, 0.07)",
  card: "rgba(255, 255, 255, 0.04)",
};
```

Import from @react-email/components:

- Html, Head, Body, Preview, Container, Section, Text, Heading, Button, Hr

BaseEmail component:

- Accepts preview: string and children: React.ReactNode
- Dark background (#08080a) on Body
- Card-like Container with rgba background, 16px border-radius, subtle border
- Max-width 480px for email client compatibility
- Export both BaseEmail component and VAULT_COLORS object

CRITICAL: All styles MUST be inline style={{ }} props. No CSS classes, no Tailwind, no external stylesheets.
</action>
<verify>grep -E "VAULT_COLORS|BaseEmail" src/emails/base-template.tsx && grep "style={{" src/emails/base-template.tsx | head -5</verify>
<done>BaseEmail component exists with Vault colors exported, all styles are inline style props</done>
</task>

<task type="auto">
  <name>Create reset password email template</name>
  <files>src/emails/reset-password.tsx</files>
  <action>
Create src/emails/reset-password.tsx using BaseEmail wrapper.

Props interface:

```typescript
interface ResetPasswordEmailProps {
  resetUrl: string;
  expiresInHours: number;
}
```

Structure:

1. Wrap with BaseEmail preview="Reset your password"
2. Heading: "Reset Your Password" (24px, bold, foreground color)
3. Text: "We received a request to reset your password. Click the button below to create a new one."
4. Button: "Reset Password" linking to resetUrl
   - Primary amber background (#f59e0b)
   - Dark text (#0a0a0b)
   - 8px border-radius, 12px vertical padding
5. Horizontal rule with border color
6. Expiry notice: "This link expires in {expiresInHours} hour(s). If you did not request this, you can safely ignore this email."
   - Muted text color (#8a8070), 14px font size

CRITICAL:

- Import BaseEmail and VAULT_COLORS from ./base-template
- All styles inline via style={{ }} props
- No CSS classes or Tailwind
- Button must have display: "inline-block" for email clients
  </action>
  <verify>grep -E "ResetPasswordEmail|expiresInHours|resetUrl" src/emails/reset-password.tsx && grep "style={{" src/emails/reset-password.tsx | head -3</verify>
  <done>ResetPasswordEmail component exists with resetUrl and expiresInHours props, includes expiry notice, all styles inline</done>
  </task>

</tasks>

<verification>
1. Verify packages installed: npm ls @react-email/components @react-email/render
2. Verify base template exports: grep "export.*BaseEmail\|export.*VAULT_COLORS" src/emails/base-template.tsx
3. Verify reset email imports base template: grep "from.*base-template" src/emails/reset-password.tsx
4. Verify no CSS classes used: grep -v "style={{" src/emails/*.tsx should return no matches with className
</verification>

<success_criteria>

- @react-email/components@1.0.8 and @react-email/render@2.0.4 installed
- BaseEmail component with VAULT_COLORS exported
- ResetPasswordEmail component using BaseEmail wrapper
- All styles are inline style={{ }} props (no className attributes)
- Expiry notice visible in reset email template
  </success_criteria>

<output>
After completion, create `.planning/phases/08-email-templates/08-01-SUMMARY.md`
</output>
