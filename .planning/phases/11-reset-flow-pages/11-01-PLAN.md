---
phase: 11-reset-flow-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [src/app/(auth)/forgot-password/page.tsx, src/components/organisms/forgot-password-form.tsx]
autonomous: true
requirements: [RESET-02]

must_haves:
  truths:
    - "User can navigate to /forgot-password"
    - "User can enter email and submit forgot-password form"
    - "User is redirected to /login after successful submission"
    - "Form shows inline validation errors"
    - "Submit button shows loading state during submission"
  artifacts:
    - path: "src/app/(auth)/forgot-password/page.tsx"
      provides: "Forgot password page route"
    - path: "src/components/organisms/forgot-password-form.tsx"
      provides: "Email input form with useActionState"
      exports: ["ForgotPasswordForm"]
  key_links:
    - from: "forgot-password-form.tsx"
      to: "forgotPasswordAction"
      via: "useActionState hook"
      pattern: "useActionState.*forgotPasswordAction"
    - from: "forgot-password-form.tsx"
      to: "/login"
      via: "router.push on success"
      pattern: "router\\.push.*login"
---

<objective>
Create forgot-password page with email input form that matches existing auth page styling.

Purpose: Entry point for password reset flow where users request reset emails.
Output: Working /forgot-password route with form component.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reset-flow-actions/10-01-SUMMARY.md

Existing patterns to follow:
@src/components/organisms/login-form.tsx
@src/app/(auth)/layout.tsx
@src/actions/auth-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ForgotPasswordForm component</name>
  <files>src/components/organisms/forgot-password-form.tsx</files>
  <action>
Create the forgot password form organism following login-form.tsx pattern EXACTLY:

1. Start with "use client" directive
2. Import useActionState from "react", useFormStatus from "react-dom", useRouter from "next/navigation", useEffect from "react"
3. Import forgotPasswordAction and ForgotPasswordState type from "@/actions/auth-actions"
4. Import Input, Button, Label from ui components, Loader2 from lucide-react, Link and Image from next

5. Create SubmitButton component using useFormStatus pattern (same as login-form.tsx):
   - disabled={pending}
   - Shows "Sending..." with Loader2 spinner when pending, "Send reset link" when idle
   - Same amber button classes: "w-full bg-amber-500 font-semibold text-black transition-all hover:bg-amber-400"

6. Create ForgotPasswordForm function component:
   - const [state, action] = useActionState(forgotPasswordAction, initialState)
   - const router = useRouter()
   - useEffect to redirect to /login when state.success is true

7. Render structure matching login-form.tsx EXACTLY:
   - Logo above card (same Image component with dark:brightness-0 dark:invert)
   - Glass card div with "glass-elevated rounded-2xl p-8"
   - h1: "Reset password" with font-syne
   - p description: "Enter your email to receive a reset link"
   - Global error display (state.error)
   - Form with single email field (Label, Input with email type)
   - SubmitButton
   - Footer link: "Remember your password? Sign in" linking to /login

8. Email field should have:
   - id="email", name="email", type="email"
   - autoComplete="email"
   - placeholder="you@example.com"

User decision (LOCKED): Redirect to login page on success, no in-page success state.
User decision (LOCKED): Inline validation errors, not toast.
</action>
<verify>npm run build succeeds, component file exists with ForgotPasswordForm export</verify>
<done>ForgotPasswordForm component exists, matches login-form.tsx styling, redirects to /login on success</done>
</task>

<task type="auto">
  <name>Task 2: Create forgot-password page route</name>
  <files>src/app/(auth)/forgot-password/page.tsx</files>
  <action>
Create the forgot-password page:

1. Import ForgotPasswordForm from "@/components/organisms/forgot-password-form"
2. Export default function ForgotPasswordPage()
3. Return the ForgotPasswordForm component

The (auth) layout provides the glass card wrapper and branding panel automatically.

Pattern: Simple server component that renders the client form organism (same as login/page.tsx and register/page.tsx).
</action>
<verify>npm run build succeeds, /forgot-password route renders form</verify>
<done>/forgot-password route exists and renders ForgotPasswordForm</done>
</task>

</tasks>

<verification>
- [ ] ForgotPasswordForm matches login-form.tsx visual styling
- [ ] Form uses useActionState with forgotPasswordAction
- [ ] Submit button shows loading state with spinner
- [ ] Redirects to /login after successful submission
- [ ] Has "Remember your password? Sign in" link to /login
</verification>

<success_criteria>
User can visit /forgot-password, enter email, submit form, and be redirected to /login.
</success_criteria>

<output>
After completion, create `.planning/phases/11-reset-flow-pages/11-01-SUMMARY.md`
</output>
