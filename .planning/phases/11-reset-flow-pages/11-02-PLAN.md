---
phase: 11-reset-flow-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  [src/app/(auth)/reset-password/page.tsx, src/components/organisms/reset-password-form.tsx]
autonomous: true
requirements: [RESET-02]

must_haves:
  truths:
    - "User can access /reset-password?token=..."
    - "Form shows error inline when token is missing/invalid"
    - "User can enter new password and confirm password"
    - "Successful reset redirects to /login with success toast"
    - "Invalid token page has link to /forgot-password"
  artifacts:
    - path: "src/app/(auth)/reset-password/page.tsx"
      provides: "Reset password page route with Suspense"
    - path: "src/components/organisms/reset-password-form.tsx"
      provides: "Password reset form with token handling"
      exports: ["ResetPasswordForm"]
  key_links:
    - from: "reset-password-form.tsx"
      to: "resetPasswordAction"
      via: "useActionState hook"
      pattern: "useActionState.*resetPasswordAction"
    - from: "reset-password-form.tsx"
      to: "URL token param"
      via: "useSearchParams"
      pattern: "searchParams\\.get.*token"
    - from: "reset-password-form.tsx"
      to: "/login"
      via: "router.push on success"
      pattern: "router\\.push.*login"
---

<objective>
Create reset-password page with form that validates tokens and allows password update.

Purpose: Landing page from email reset links where users set new password.
Output: Working /reset-password?token=... route with form component.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reset-flow-actions/10-02-SUMMARY.md

Existing patterns to follow:
@src/components/organisms/login-form.tsx
@src/app/(auth)/layout.tsx
@src/actions/auth-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResetPasswordForm component</name>
  <files>src/components/organisms/reset-password-form.tsx</files>
  <action>
Create the reset password form organism following login-form.tsx pattern:

1. Start with "use client" directive
2. Import useActionState from "react", useFormStatus from "react-dom", useSearchParams, useRouter from "next/navigation", useEffect from "react"
3. Import resetPasswordAction and ResetPasswordState type from "@/actions/auth-actions"
4. Import toast from "sonner"
5. Import Input, Button, Label, Loader2, Link, Image (same as login-form)

6. Create SubmitButton component (same pattern):
   - "Resetting..." with spinner when pending, "Reset password" when idle

7. Create ResetPasswordForm function component:
   - const searchParams = useSearchParams()
   - const token = searchParams.get("token") || ""
   - const router = useRouter()
   - const [state, action] = useActionState(resetPasswordAction, initialState)
   - useEffect to redirect and show toast when state.success is true:
     ```tsx
     useEffect(() => {
       if (state.success) {
         toast.success("Password reset successful, please log in");
         router.push("/login");
       }
     }, [state.success, router]);
     ```

8. If no token, render error state:
   - Glass card with error: "This reset link is invalid or has expired."
   - Link to /forgot-password: "Request a new reset link"

9. If token exists, render form matching login-form.tsx styling:
   - Logo above card
   - Glass card: "glass-elevated rounded-2xl p-8"
   - h1: "Set new password" with font-syne
   - p: "Enter your new password below"
   - Global error display (state.error) WITH link to /forgot-password inside error div
   - Hidden input: `<input type="hidden" name="token" value={token} />`
   - Password field: id="password", name="password", type="password", autoComplete="new-password"
   - Password hint: "Min. 8 characters, at least one number"
   - Field error: state.fieldErrors?.password
   - Confirm password field: id="confirmPassword", name="confirmPassword", type="password"
   - Field error: state.fieldErrors?.confirmPassword
   - SubmitButton

User decision (LOCKED): Unified error message "This reset link is invalid or has expired" - no distinction between invalid/expired/used.
User decision (LOCKED): Show error directly on page, don't redirect away.
User decision (LOCKED): Link to forgot-password in error state.
</action>
<verify>npm run build succeeds, component handles token param, shows error without token</verify>
<done>ResetPasswordForm exists, validates token, submits to resetPasswordAction, redirects with toast on success</done>
</task>

<task type="auto">
  <name>Task 2: Create reset-password page route with Suspense</name>
  <files>src/app/(auth)/reset-password/page.tsx</files>
  <action>
Create the reset-password page with Suspense boundary (required for useSearchParams):

```tsx
import { Suspense } from "react";
import { ResetPasswordForm } from "@/components/organisms/reset-password-form";

export default function ResetPasswordPage() {
  return (
    <Suspense fallback={<div className="text-muted-foreground">Loading...</div>}>
      <ResetPasswordForm />
    </Suspense>
  );
}
```

The Suspense boundary is REQUIRED because useSearchParams() in ResetPasswordForm needs it in Next.js App Router.
</action>
<verify>npm run build succeeds, /reset-password?token=test renders form without hydration errors</verify>
<done>/reset-password route exists with Suspense boundary, renders ResetPasswordForm</done>
</task>

</tasks>

<verification>
- [ ] ResetPasswordForm matches login-form.tsx visual styling
- [ ] Form extracts token from URL params
- [ ] Missing token shows error with forgot-password link
- [ ] Form submits token as hidden field
- [ ] Successful reset shows toast and redirects to /login
- [ ] Page has Suspense boundary for useSearchParams
</verification>

<success_criteria>
User can visit /reset-password?token=xyz, enter new password, and be redirected to /login with success toast. Invalid/missing token shows inline error.
</success_criteria>

<output>
After completion, create `.planning/phases/11-reset-flow-pages/11-02-SUMMARY.md`
</output>
