---
phase: 12-welcome-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/auth-actions.ts
autonomous: true
requirements:
  - WELCOME-01
  - WELCOME-02
  - WELCOME-03
user_setup: []
must_haves:
  truths:
    - "Welcome email is sent after successful registration (if SMTP enabled)"
    - "Registration completes without waiting for email to send"
    - "Email send failure does not affect registration success"
  artifacts:
    - path: "src/actions/auth-actions.ts"
      provides: "Welcome email integration in registerAction"
      contains: "renderWelcomeEmail"
  key_links:
    - from: "src/actions/auth-actions.ts:registerAction"
      to: "src/emails/index.tsx:renderWelcomeEmail"
      via: "import and async call"
      pattern: "renderWelcomeEmail"
    - from: "registerAction"
      to: "src/lib/email/transporter.ts:sendEmail"
      via: ".then() chain"
      pattern: "sendEmail.*to.*email"
---

<objective>
Integrate welcome email sending into registerAction after successful user creation.

Purpose: New users receive a branded welcome email after registration, without blocking the registration flow or causing failures if email sending fails.
Output: Modified registerAction with non-blocking welcome email integration.
</objective>

<execution_context>
@/home/coder/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/coder/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-welcome-email/12-RESEARCH.md
@src/actions/auth-actions.ts
@src/lib/email/transporter.ts
@src/emails/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add welcome email integration to registerAction</name>
  <files>src/actions/auth-actions.ts</files>
  <action>
    Add non-blocking welcome email sending to registerAction after successful user insertion.

    **Implementation steps:**

    1. Add `renderWelcomeEmail` to the existing `@/emails` import on line 19:
       ```typescript
       import { renderResetPasswordEmail, renderWelcomeEmail } from "@/emails";
       ```

    2. After the `db.insert(users)` call at line 110, add welcome email sending:
       ```typescript
       // Send welcome email (non-blocking, failures ignored)
       if (isEmailConfigured()) {
         const userName = email.split("@")[0];
         renderWelcomeEmail(userName)
           .then(({ html, text, subject }) => sendEmail({ to: email, subject, html, text }))
           .catch((error) => {
             logger.error("Failed to send welcome email", "auth", error);
           });
       }
       ```

    **Critical requirements:**
    - Use `.then()/.catch()` pattern, NOT `await` — registration must continue immediately
    - Derive userName from email local part (before @) since name field is optional
    - Wrap email logic in `isEmailConfigured()` check
    - Log errors with logger.error but never throw or return error state
    - Place after db.insert but before signIn call

    **Reference:** Follow the pattern from `forgotPasswordAction` (lines 159-177) which demonstrates the correct non-blocking email pattern with graceful failure handling.

  </action>
  <verify>
    grep -n "renderWelcomeEmail" src/actions/auth-actions.ts && grep -n "isEmailConfigured" src/actions/auth-actions.ts | head -5
  </verify>
  <done>
    - renderWelcomeEmail is imported from @/emails
    - Welcome email sending code exists after db.insert call
    - Uses fire-and-forget pattern (no await)
    - Errors are caught and logged, not thrown
    - Registration flow is unaffected by email outcome
  </done>
</task>

</tasks>

<verification>
1. Check that renderWelcomeEmail is imported from @/emails
2. Verify welcome email code is placed after db.insert but before signIn
3. Confirm fire-and-forget pattern (no await on email send)
4. Verify error handling with logger.error
5. Run `npm run build` to ensure no type errors
</verification>

<success_criteria>

- Welcome email is sent after successful registration (if SMTP enabled)
- Registration completes without waiting for email to send
- Email send failure does not affect registration success
- Code follows existing patterns from forgotPasswordAction
  </success_criteria>

<output>
After completion, create `.planning/phases/12-welcome-email/12-01-SUMMARY.md`
</output>
